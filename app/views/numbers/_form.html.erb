<%= simple_form_for(@number) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :n_type %>
    <%= f.input :start_value %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
