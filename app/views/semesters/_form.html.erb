<%= simple_form_for @semester, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :group_id %>
  <%= f.input :semester %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                semesters_path, :class => 'btn' %>
  </div>
<% end %>
