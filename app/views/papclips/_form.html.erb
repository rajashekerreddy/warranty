<%= simple_form_for @papclip, :html => { :multipart => true } do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.file_field :files %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
